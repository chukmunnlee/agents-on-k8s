[project]
name = "agents-on-k8s"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Lee Chuk Munn", email = "chukmunnlee@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "k8s-agent-sandbox>=1.0.3",
    "smolagents[docker,gradio,mcp,openai,telemetry,toolkit]>=1.26.0",
]

[project.scripts]
agents-on-k8s = "agents_on_k8s:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
